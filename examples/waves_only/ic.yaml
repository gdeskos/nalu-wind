# -*- mode: yaml -*-

nalu_preprocess:

  input_db: waves_only.exo

  output_db: waves_only_ic.exo

  tasks:
    - init_couette_fields

  init_couette_fields:
    fluid_parts: [fluid_part]

    velocity:
      Re_tau : 130 
      U0 : 1.0
      viscosity : 0.000125   
    #temperature:  
    #  Ri : 0.031
    #  T0 : 300. 
